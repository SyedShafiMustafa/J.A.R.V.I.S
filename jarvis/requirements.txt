# --- AI brain (local, free) ---
requests                  # Ollama API calls (streaming chat) — the "ollama" pip package is NOT used

# --- Voice: wake word + speech-to-text + text-to-speech ---
openwakeword              # wake word detection ("Hey Jarvis"), runs offline
faster-whisper            # local speech-to-text, fast + accurate
piper-tts                 # local JARVIS voice synthesis (models/piper)
pyaudio                   # microphone input stream
sounddevice               # audio playback/recording helper
soundfile                 # WAV writing for voice recordings
numpy                     # audio array math

# --- Memory ---
chromadb                  # local vector database — semantic memory (roadmap)

# --- Desktop control ---
pyautogui                 # move mouse, click, type, screenshot
pygetwindow               # find/focus/list open windows
pyperclip                 # clipboard read/write
pywinauto                 # Windows UI automation (active window controls)
pywin32                   # win32gui used by ui_automation

# --- Vision ---
pytesseract               # OCR on screenshots (also requires the Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki)
Pillow                    # image handling for screenshots

# --- UI (roadmap — orb window not built yet) ---
PySide6                   # the futuristic desktop interface

# --- Utilities ---
python-dotenv             # load settings from .env file